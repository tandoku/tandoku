# TextBlock
text: markdown
contentKind: primary <default> | secondary | meta | sound-effect | on-screen-text | lyrics | dialogue | narration | heading
actor: text
image:
  name: filename
  regions: TODO # or maybe region? populate this on nested blocks from OCR?
references:
  <ref-name>:
    text: markdown
    actor: text
    image: image
source:
  resource: text
  timecodes: {start: timespan, end: timespan}
---
# CompositeBlock
blocks:
  - TextBlock
image: image